DATABASE_URL=postgres://user:password@host:5432/dbname

# External dispatch system DB we read from (read-only), scoped to one company/tenant.
TARGET_DB_URL=postgres://user:password@host:5432/dispatch
TARGET_DB_COMPANY_ID=

MONDAY_API_TOKEN=
MONDAY_BOARD_ID=
# "Fuel Board 2.0" — items (trucks) are created/deleted by the other side, not by us.
MONDAY_SECONDARY_BOARD_ID=

SAMSARA_API_TOKEN=

GOOGLE_SERVICE_ACCOUNT_JSON=/path/to/service-account.json
GOOGLE_SHEET_ID=
